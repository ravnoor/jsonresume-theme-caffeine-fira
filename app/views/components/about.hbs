{{#resume.basics}}
<div class="container about-container">
  {{> title value="About"}}

  {{#if location}}
    {{#location}}
    {{> info-tag text=(formatAddress address city region postalCode countryCode) fa-icon="fa-map-marker"}}
    {{/location}}
  {{/if}}

  {{#if email}}
    {{> info-tag text=this.email fa-icon="fa-envelope-o"}}
  {{/if}}

  {{#if phone}}
    {{> info-tag text=this.phone fa-icon="fa-mobile"}}
  {{/if}}

  {{#if website}}
    {{> info-tag text=this.website fa-icon="fa-desktop" url=this.website}}
  {{/if}}

  {{#if profiles}}
    {{#each profiles}}
        {{> social}}
    {{/each}}
  {{/if}}
</section>
{{/resume.basics}}
