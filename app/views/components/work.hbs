{{#if resume.work.length}}
<div class="container work-container">
  {{> title value="Experience"}}

  {{#each resume.work}}
  <section class="item">
    {{#if position}}
      <h3 class="smallcaps bold">{{position}}</h3>
    {{/if}}
    {{#if position}}
      {{> section-header name=this.company }}
    {{/if}}

    {{#if summary}}
      <p class="summary">{{summary}}</p>
    {{/if}}

    {{#if highlights.length}}
      <ul>
        {{#each highlights}}
        <li>{{{.}}}</li>
        {{/each}}
      </ul>
    {{/if}}
  </section>
  {{/each}}
</div>
{{/if}}
